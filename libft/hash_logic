
pour allouer une table de hash je n'ai besoin que de l'alloc de base et de la
	fonction de hash
	---func alloc_new_table(h_func)
		if (!(new_table = (t_h_table *)easymalloc(sizeof(*new_table))))
			return (0);
		new_table->func = h_func;
		if (!(table->array = resize_array(new_table)))

le tableau devrait venir d'une fonction array upsize qui verifie si size > 0,
	si il n'existe pas elle prend 503 sinon find next prime de size * 3
	lees limites doivent etre mises a jour comme des variables de la taille du
		tableau, et le diviseur lui peut etre un define, exemple:
		---func resize_array(h_table)
			new_size = ft_find_next_prime(current_size * 3)
			if (!(new_array = rehash_array(current_array, current_size, new_size)))
				return (0);
			table->size = new->size;
			easyfree(table->array);
			table->array = new_array;
			table->elems_limit = table->size / ELEMS_LIMIT;
			table->collisions_limit = table->size / COLLISIONS_LIMIT;


